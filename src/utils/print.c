#include "minishell.h"

/* Debug functions removed for tester compatibility */
void print_ast(t_node *node, int level)
{
    (void)node;
    (void)level;
    /* Function body removed for tester compatibility */
}
